mg_name = v7
max_block_send_distance = 10
active_block_range = 4
creative_mode = false
enable_damage = true
time_speed = 72

# Note: `secure.http_mods = relm_wallet,relm_rewards` must live in the
# engine's root/user minetest.conf (NOT this game conf — security
# settings are rejected when shipped inside a game). See README.
