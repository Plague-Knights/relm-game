mg_name = v7
max_block_send_distance = 10
active_block_range = 4
creative_mode = false
enable_damage = true
time_speed = 72

# Mods that are allowed to make outbound HTTP requests.
# Relm's wallet and rewards modules talk to the backend for on-chain
# actions, so they have to be listed here or the HTTP API is withheld.
secure.http_mods = relm_wallet,relm_rewards
